# Pinned exact versions (verified against .venv). Do not use ranges here:
# reproducible deploys must resolve to bit-identical dependencies.
fastapi==0.141.1
uvicorn[standard]==0.53.0
sqlalchemy==2.1.0
geoalchemy2==0.20.0
psycopg[binary]==3.3.6
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
